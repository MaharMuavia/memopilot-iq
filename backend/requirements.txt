# --- Core ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
python-dotenv==1.0.1
numpy>=1.26  # vectorized dense retrieval (optional at runtime, listed for scale)

# --- Tests ---
pytest==8.3.4
pytest-asyncio==0.25.2

# --- Alibaba Cloud ---
# Kept in the standard install so ECS and Function Compute use the same
# dependency set as local development when ALIBABA_CLOUD_MODE is enabled.
tablestore==6.4.8
oss2==2.19.1

# --- Optional local vector backends (brute-force cosine is the default) ---
# faiss-cpu==1.9.0
# chromadb==0.5.23
